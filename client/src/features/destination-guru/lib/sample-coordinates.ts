// Lat/lng for the built-in SAMPLE_DATA fixtures in components/destination-guru.tsx,
// keyed exactly like SAMPLE_DATA so the sample entries can still be pinned on
// the globe before any AI-backed (DB) row exists for them.
export const SAMPLE_COORDINATES: Record<string, { lat: number; lng: number }> = {
  Corfu: { lat: 39.6243, lng: 19.9217 },
  "Sharm El Sheikh": { lat: 27.9158, lng: 34.33 },
};
